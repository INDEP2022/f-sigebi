const ERROR_GOOD_NULL = `Ingresa un número de Bien válido`;
const NOT_FOUND_GOOD = (error: string) =>
  `Error al cargar la información del Bien. ${error}`;

export { ERROR_GOOD_NULL, NOT_FOUND_GOOD };
