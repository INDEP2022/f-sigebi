export interface IRateCatalog {
  keyRate: string;
  year: number;
  month: string;
  coinType: string;
  rate: string;
  closed: string;
  rateClosingDate: string;
  userClosingRate: string;
  registerNumber: string;
}
