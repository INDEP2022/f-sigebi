export interface PBDelete {
  contEli: number;
  selEli: number;
  aggregate: string;
  processingArea: string;
  states: string;
  user: string;
  actaNumber: number;
  // processingArea: string | null;
  // states: string | null;
  // selEli: number;
  // actaNumber: number;
  // user: string | null;
  // contEli:number;
  // aggregate:string
}
