export interface PBDelete {
  aggregate: string;
  contEli: number;
  processingArea: string;
  selEli: number;
  states: string;
  user: string;

  // processingArea: string | null;
  // states: string | null;
  // selEli: number;
  // actaNumber: number;
  // user: string | null;
  // contEli:number;
  // aggregate:string
}
